#include <iostream>

using namespace std;

/*
* IntStringTable using a double pointer to a LinkedList
* and two variables, capacity and elementQuantity.
* Add, erase, isDefined and recovery operations are made in O(1) on average.
* Open hashing is used with module operation suited as hash function.
* Implementation is unbounded.
*/

struct TableNode {

};

int main() {
    return 0;
}